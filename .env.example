# Необязательно.
# По умолчанию БД лежит в ~/.purchase_tracker_mcp/purchases.sqlite3
PURCHASE_DB_PATH=~/.purchase_tracker_mcp/purchases.sqlite3

# stdio — удобно для локального MCP-клиента.
# streamable-http — если нужен HTTP transport.
MCP_TRANSPORT=stdio

PURCHASE_MCP_LOG_LEVEL=INFO
